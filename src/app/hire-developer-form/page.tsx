import HireDeveloperForm from "@/components/HireDevelopers/HireDeveloperForm";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <main>
      <HireDeveloperForm />
    </main>
  );
};

export default page;
